# TWSE 12:00 盤中策略分析

日期：2026-04-30  
分析時間：12:22 左右  
性質：研究紀錄，不保證收益

## 摘要

- `2026-04-30 12:22` 已在工作區再次完成全量資料整理與回測，`twse_backtest_report.md` 更新時間為 `2026-04-30 12:22:03`；但本輪可重複驗證的最新收盤資料仍停留在 `2026-04-23`，因此這份 12:00 盤中分析不宣稱已取得 `2026-04-24` 到 `2026-04-30` 的官方日線回測數值。
- 回測主報告顯示執行日與最新可驗證收盤日之間已擴大到 `7` 個日曆天落差；這代表今天 12:00 的正式判讀仍屬資料凍結狀態，只能延用最近一個可驗證收盤版本，不把盤中價格寫入回測 NAV。
- 本輪再次核對 `twse_monthly_holdings.csv` 與 `twse_prices_close.csv`，確認最新已驗證持倉仍為 `006208 50% / AI 35% / 00631L 10% / 現金 5%`，且 AI 動能前五名仍為 `2383 / 2308 / 2345 / 2454 / 2330`。
- 外部交叉檢查可找到不同步快照：Yahoo 搜尋摘要顯示 `00631L` 在 `2026-04-29 14:30` 收於 `28.82`，`006208` 在 `2026-04-24 14:30` 收於 `208.95`，`0050` 可驗證到的 Yahoo 快照也只停留在 `2026-04-24`；由於核心基準的新鮮度明顯落後於高 beta 標的，本輪把這些數字全部列為監控旁證，不進入規則主鏈。
- 本輪新增的實質規則是「核心基準新鮮度閘門」：若 `00631L` 的外部快照時間明顯新於 `0050` / `006208`，且差距已跨越多個交易時段，則即使 `00631L` 顯示強弱變化，也不得單獨觸發槓桿開關或改寫 `risk-on/risk-off`。

## 今日判讀

### 市場 regime

- `risk-on` 維持。
- `006208` 核心 `50%` 不變。
- `00631L` 續留上限 `10%`，但不加碼。
- 現金緩衝維持 `5%`。
- 因本地可驗證日線仍停在 `2026-04-23`，且外部快照新鮮度不同步，本輪不依單一外部價格翻轉 regime。

### AI 籃子

當前主策略持倉仍為：

- `2308`、`2330`、`2345`、`2383`、`2454` 各 `7%`

本輪前五名排名未變，因此沒有新的收盤版訊號支持換股。

### 風控重點

- 歷史最大回撤 `-24.87%` 仍接近內部觀察線 `-25%`，槓桿紀律維持不變。
- 當本地可驗證收盤資料落後超過 `2` 個交易日，策略只允許做監控與回測重跑，不允許依未驗證外部數字改變候選池、槓桿或現金權重。
- 當外部資料源落後超過 `3` 個日曆天，盤中與盤後紀錄都必須同步附上「本地最新實際收盤日」與「策略狀態凍結」說明。
- 新增核心基準新鮮度閘門：若 `00631L` 的外部快照比 `0050` / `006208` 新，但核心基準拿不到同日或更近的可重現旁證，則該 `00631L` 訊號只能視為雜訊監控，不得觸發加減碼。
- 若後續 `0050` 收盤跌破 `60` 日均線，或 `20` 日均線下穿 `60` 日均線，`00631L` 應先歸零。
- 若主策略實際回撤擴大到 `-35%`，應切換到防守模式；若擴大到 `-40%`，應視為結構失效並停止把它當主方案。
- 若補入總報酬、月營收或更細成本後，主策略年化報酬掉到 `20%` 以下，需把主策略降回候選。

## 假設與限制

- 本地回測資料截止 `2026-04-23` 收盤。
- 回測為價格報酬，未納入股息再投入。
- 交易成本壓力測試仍使用每 `1.00` 換手成本 `0.20%` 的粗略假設，尚未拆分股票、ETF、證交稅與滑價。
- 本輪主要資料來源仍是工作區本地 `FinMind TaiwanStockPrice` 快取、`twse_prices_close.csv`、`twse_backtest_summary.csv`、`twse_monthly_holdings.csv` 與 `twse_backtest_report.md`。
- API 抽查與本地重跑都顯示，在請求到 `2026-04-30` 時，回測可驗證日線仍只落到 `2026-04-23`；今天不把任何未落地盤中成交直接混入回測淨值。
- TWSE `MI_INDEX` 與 `MI_5MINS_INDEX` endpoint 在當前 shell 環境仍未提供可重複利用的有效內容，本紀錄把它視為資料源限制，而不是市場異常訊號。
- 外部定性參照使用 Yahoo 股市搜尋快照；由於 `00631L`、`006208`、`0050` 的可見時間戳不一致，這些數字只作監控旁證，不進入回測或規則判斷主鏈。

## 來源

- 工作區本地檔案：`twse_backtest_report.md`、`twse_backtest_summary.csv`、`twse_monthly_holdings.csv`、`twse_prices_close.csv`
- 回測腳本：`twse_backtest.py`
- 價格快取：`data/finmind/*`
- API 抽查：`FinMind TaiwanStockPrice`
- 外部盤中參照：Yahoo 股市搜尋快照（`00631L` 資料時間 `2026-04-29 14:30`、`006208` 資料時間 `2026-04-24 14:30`、`0050` 可驗證摘要停留在 `2026-04-24`）
