# TWSE 10:00 盤中策略分析

日期：2026-04-30  
分析時間：12:25 左右  
性質：研究紀錄，不保證收益

## 摘要

- `2026-04-30 12:25` 已在工作區重跑 `.venv/bin/python twse_backtest.py`，`[twse_backtest_report.md](/Users/yhsung/Documents/Codex/2026-04-19-computer-use-plugin-computer-use-openai-2/twse_backtest_report.md)` 更新時間為 `2026-04-30 12:25:48`；但本輪可重複驗證的最新收盤資料仍停留在 `2026-04-23`，因此今日 `10:00` 盤中分析只延用最近可驗證收盤版本，不把盤中價格寫入回測 NAV。
- 主策略 `006208核心現金防守 50/35/10（成本0.20%）` 回測結果維持年化 `22.47%`、最大回撤 `-24.87%`、Sharpe `1.23`；AI 動能前五名仍為 `2383 / 2308 / 2345 / 2454 / 2330`，沒有新的收盤版訊號支持換股。
- 今天新增的研究進展不是績效數字，而是明確把「核心訊號新鮮度」獨立揭露：雖然部分 FinMind 快取檔名已到 `2026-04-24`，但實際可交易的最後有效收盤對 `0050 / 006208 / 00631L` 仍是 `2026-04-23`，所以正式風控與權重判讀必須以核心標的最後有效收盤日為準，而不是看快取檔名或請求的 `end_date`。
- 外部頁面交叉檢查沒有提供可用的 `2026-04-30 10:00` 同步旁證：Yahoo 股市公開頁目前可抓到的 `0050`、`006208`、`00631L` 顯示日期仍停在 `2026-02-10` 到 `2026-02-11`；因此本輪不把這些外部頁面當成盤中驗證源，只把它們視為「外部可見頁面本身陳舊」的證據。

## 今日判讀

### 市場 regime

- `risk-on` 維持，但僅限於 `2026-04-23` 最近可驗證收盤所對應的 regime。
- `006208` 核心 `50%` 不變。
- `00631L` 續留上限 `10%`，但不加碼。
- 現金緩衝維持 `5%`。
- `0050` 於最近可驗證收盤 `86.35` 仍高於 `20` / `60` / `200` 日均線，且 `20` 日均線仍高於 `60` 日均線。

### AI 籃子

當前主策略持倉仍為：

- `2308`、`2330`、`2345`、`2383`、`2454` 各 `7%`

本輪前五名未變，因此沒有新的收盤版訊號支持換股。

### 風控重點

- 歷史最大回撤 `-24.87%` 仍接近內部觀察線 `-25%`，`00631L` 只能維持上限 `10%`。
- 當本地可驗證收盤資料落後超過 `2` 個交易日，策略只允許做監控與回測重跑，不允許依未驗證外部數字翻轉候選池、槓桿或現金權重。
- 當外部資料源落後超過 `3` 個日曆天，盤中或盤後紀錄都必須同步附上「本地最新實際收盤日」與「策略狀態凍結」說明。
- 新增核心訊號風控：若 `0050`、`006208`、`00631L` 的最後有效收盤日沒有同步前進，即使快取檔名較新，也不得視為策略資料已更新。
- 若後續 `0050` 收盤跌破 `60` 日均線，或 `20` 日均線下穿 `60` 日均線，`00631L` 應先歸零。
- 若主策略實際回撤擴大到 `-35%`，應切換到防守模式；若擴大到 `-40%`，應視為結構失效並停止把它當主方案。

## 假設與限制

- 本地回測資料截止 `2026-04-23` 收盤。
- 回測為價格報酬，未納入股息再投入。
- 交易成本壓力測試仍使用每 `1.00` 換手成本 `0.20%` 的粗略假設，尚未拆分股票、ETF、證交稅與滑價。
- 本輪主要資料來源仍是工作區本地 `FinMind TaiwanStockPrice` 快取、`twse_prices_close.csv`、`twse_backtest_summary.csv`、`twse_monthly_holdings.csv` 與 `[twse_backtest_report.md](/Users/yhsung/Documents/Codex/2026-04-19-computer-use-plugin-computer-use-openai-2/twse_backtest_report.md)`。
- 外部頁面只做資料可得性檢查，不進入規則主鏈；本輪 Yahoo 股市頁面對 `0050`、`006208`、`00631L` 的可見時間分別停在 `2026-02-11 14:30`、`2026-02-10 11:19`、`2026-02-11 14:30`。

## 來源

- 工作區本地檔案：`twse_backtest_report.md`、`twse_backtest_summary.csv`、`twse_monthly_holdings.csv`、`twse_prices_close.csv`
- 回測腳本：`twse_backtest.py`
- 價格快取：`data/finmind/*`
- API 與本地快取交叉檢查：`FinMind TaiwanStockPrice`
- 外部可見頁面檢查：Yahoo 股市公開 quote 頁（`0050`、`006208`、`00631L`）
