# TWSE 收盤後策略分析

日期：2026-05-07  
分析時間：18:08 左右  
性質：研究紀錄，不保證收益

## 摘要

- `2026-05-07 18:08` 已在工作區完成 `.venv/bin/python twse_backtest.py` 收盤後全量重跑；`twse_backtest_report.md` 更新時間為 `2026-05-07 18:08:39`，但本輪可重複驗證的最新收盤資料仍停留在 `2026-04-23`，因此今天的收盤後分析不宣稱已取得 `2026-04-24` 到 `2026-05-07` 的官方日線回測數值。
- 依 TWSE 常態交易機制，`2026-05-07` 一般交易於 `13:30` 收盤；因此本輪屬於正常交易日收盤後檢查，不是盤中監控。
- 回測主報告顯示執行日與最新可驗證收盤日之間已有 `14` 個日曆天落差；本輪正式結論因此受「正常交易日已收盤」與「資料源延遲」兩層條件共同約束。
- 本輪維持資料模式為 `monitor-only`，且正式升級資料治理狀態為 `source-remediation-required`：核心訊號 `0050 / 006208 / 00631L` 的最後有效收盤日落後執行日達 `14` 個日曆天，加上最新 `FinMind` 快取都出現 `filename-ahead`，後續輪次不能只做重跑記錄，必須先修資料來源。
- 本輪新增可重複驗證的快取稽核表：`0050 / 006208 / 00631L / 2308 / 2330 / 2383` 的最新快取檔名都已到 `*_2026-04-24.json`，但 payload 最後一筆日期仍全部停在 `2026-04-23`，檔名超前內容 `1` 天。
- 以 `2026-04-23` 最近可驗證收盤檢查，`0050` 仍高於 `20` / `60` / `200` 日均線，且 `20` 日均線高於 `60` 日均線；`00631L` 的 regime 啟用條件在最後已驗證收盤上仍成立，但收盤後也不得因未驗證價格放寬部位。
- 主策略 `006208核心現金防守 50/35/10（成本0.20%）` 回測結果仍為年化 `22.47%`、最大回撤 `-24.87%`、Sharpe `1.23`；AI 動能籃子前 `5` 名仍是 `2383 / 2308 / 2345 / 2454 / 2330`。
- 與 `2026-05-06` 收盤後版本相比，本輪主要進展不是績效數字，而是把資料治理從文字規則升級為報告內建稽核與 remediation 觸發；資料截止日、候選池、主策略績效與權重仍相同，因此本輪依舊不是新的策略訊號版本。

## 今日判讀

### 市場 regime

- `risk-on` 維持，但僅限於最近可驗證收盤 `2026-04-23` 的框架下成立。
- `006208` 核心維持 `50%`。
- AI 動能籃子維持 `35%`，沿用最後已驗證前五名。
- `00631L` 維持上限 `10%`，但資料凍結期間不加碼、不減碼。
- 現金緩衝維持 `5%`。
- 雖然今天已收盤，但因核心日線仍停留在 `2026-04-23`，本輪不允許翻轉 regime 或放寬槓桿。

### AI 籃子

當前主策略持倉仍為：

- `2308`、`2330`、`2345`、`2383`、`2454` 各 `7%`

本輪前五名未變，資料凍結期間不允許依外部頁面快照重排候選池。

### 00631L 啟用條件

- 依最後已驗證收盤，`0050` 高於 `200` 日均線，且 `20` 日均線高於 `60` 日均線。
- `0050` 近 `63` 個交易日動能約 `+22.74%`，所以 `00631L` 的 regime 啟用條件仍成立。
- 但本輪資料模式維持 `monitor-only`，且治理狀態已升級為 `source-remediation-required`，因此 `00631L` 只能維持既有上限 `10%`，不能因未驗證收盤價、外部頁面或較新的快取檔名推高部位。

### 風控重點

- 歷史最大回撤 `-24.87%` 仍接近內部觀察線 `-25%`，因此 `00631L` 只能維持上限 `10%`。
- 當核心日線滯後達 `5` 個日曆天以上，策略自動切換為 `monitor-only`；今天的 `14` 天滯後已明確落在這個區間內。
- 本輪新增的資料治理證據是：快取檔名雖前進到 `2026-04-24`，但 payload 仍停在 `2026-04-23`；權重判讀必須以 payload 最後有效日期為準。
- 只要 `0050 / 006208 / 00631L` 的最後有效收盤日沒有前進，收盤後自動化也不得依外部數字改變候選池、槓桿或現金權重。
- 本輪正式定義 remediation 門檻：核心訊號滯後達 `14` 個日曆天，或最新快取出現 `filename-ahead`，下一輪就必須優先驗證 live API payload 最後日期，再決定是否改接替代日線來源。
- 若後續 `0050` 收盤跌破 `60` 日均線，或 `20` 日均線下穿 `60` 日均線，`00631L` 應先歸零。
- 若主策略實際回撤擴大到 `-35%`，應切換到防守模式；若擴大到 `-40%`，應視為結構失效並停止把它當主方案。
- 若補入總報酬、月營收或更細成本後，主策略年化報酬掉到 `20%` 以下，需把主策略降回候選。

## 假設與限制

- 本地回測資料截止 `2026-04-23` 收盤。
- 回測為價格報酬，未納入股息再投入。
- 交易成本壓力測試仍使用每 `1.00` 換手成本 `0.20%` 的粗略假設，尚未拆分股票、ETF、證交稅與滑價。
- 本輪主要資料來源仍是工作區本地 `FinMind TaiwanStockPrice` 快取、`twse_prices_close.csv`、`twse_backtest_summary.csv`、`twse_monthly_holdings.csv` 與 `twse_backtest_report.md`。
- 官方市場狀態只用來判定今天已進入收盤後，不把任何外部價格寫入回測主鏈。
- 下一輪應優先拆檢 `FinMind` API 回傳日期、`parse_ticker_finmind()` 的快取新鮮度判斷與替代日線來源，而不是反覆根據新時間戳產生無增量版本。

## 來源

- 工作區本地檔案：`twse_backtest_report.md`、`twse_backtest_summary.csv`、`twse_monthly_holdings.csv`、`twse_prices_close.csv`
- 回測腳本：`twse_backtest.py`
- 價格快取：`data/finmind/*`
- 官方市場狀態：[TWSE Trading Mechanism](https://www.twse.com.tw/en/page/products/trading/introduce.html)、[TWSE Holiday Schedule](https://www.twse.com.tw/en/holidaySchedule/holidaySchedule)
- API 與快取交叉檢查：`FinMind TaiwanStockPrice`
