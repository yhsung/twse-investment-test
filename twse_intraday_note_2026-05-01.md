# TWSE 10:00 策略分析

日期：2026-05-01  
分析時間：10:07 左右  
性質：研究紀錄，不保證收益

## 摘要

- `2026-05-01 10:07` 已在工作區再次完成 `.venv/bin/python twse_backtest.py` 全量重跑；但本輪可重複驗證的最新收盤資料仍停留在 `2026-04-23`，因此今天的 `10:00` 分析不宣稱已取得 `2026-04-24` 到 `2026-05-01` 的官方日線回測數值。
- 今天 `2026-05-01` 依 TWSE 官方 `114` 年市場開休市日期表為勞動節休市，所以這份報告屬於「休市日狀態檢查」，不是盤中追價紀錄；任何外部頁面的零碎報價都不得進入策略主鏈。
- 回測主報告顯示執行日與最新可驗證收盤日之間已有 `8` 個日曆天落差；這表示本輪正式結論同時受 `休市` 與 `資料源延遲` 兩層限制約束。
- 以 `2026-04-23` 最近可驗證收盤檢查，`0050` 仍高於 `20` / `60` / `200` 日均線，且 `20` 日均線高於 `60` 日均線；`00631L` 的 regime 啟用條件仍成立，但休市日不得加減碼。
- 主策略 `006208核心現金防守 50/35/10（成本0.20%）` 回測結果仍為年化 `22.47%`、最大回撤 `-24.87%`、Sharpe `1.23`；AI 動能籃子前 `5` 名仍是 `2383 / 2308 / 2345 / 2454 / 2330`，沒有新的收盤版訊號支持換股。
- 本輪新增的正式流程規則是：若 TWSE 官方開休市表顯示休市，`10:00` 自動化一律進入 `monitor-only` 模式，只沿用上一個已驗證收盤版本檢查風控與資料完整性。

## 今日判讀

### 市場 regime

- `risk-on` 維持，但僅限於 `2026-04-23` 最近可驗證收盤對應的 regime。
- `006208` 核心 `50%` 不變。
- `00631L` 續留上限 `10%`，但休市日不加碼、不減碼。
- 現金緩衝維持 `5%`。
- 因今天官方休市且本地日線未前進，本輪不允許翻轉 regime、重排 AI 籃子或放寬槓桿。

### AI 籃子

當前主策略持倉仍為：

- `2308`、`2330`、`2345`、`2383`、`2454` 各 `7%`

本輪前五名未變，且休市日不允許依外部頁面重排候選池。

### 風控重點

- 歷史最大回撤 `-24.87%` 仍接近內部觀察線 `-25%`，`00631L` 只能維持上限 `10%`。
- 當 TWSE 官方開休市表顯示休市，所有盤中自動化只能做監控與資料完整性檢查，不允許依外部報價翻轉候選池、槓桿或現金權重。
- 當本地可驗證收盤資料落後超過 `2` 個交易日，策略只允許做監控與回測重跑，不允許依未驗證外部數字改變權重。
- 若後續 `0050` 收盤跌破 `60` 日均線，或 `20` 日均線下穿 `60` 日均線，`00631L` 應先歸零。
- 若主策略實際回撤擴大到 `-35%`，應切換到防守模式；若擴大到 `-40%`，應視為結構失效並停止把它當主方案。
- 若補入總報酬、月營收或更細成本後，主策略年化報酬掉到 `20%` 以下，需把主策略降回候選。

## 假設與限制

- 本地回測資料截止 `2026-04-23` 收盤。
- 回測為價格報酬，未納入股息再投入。
- 交易成本壓力測試仍使用每 `1.00` 換手成本 `0.20%` 的粗略假設，尚未拆分股票、ETF、證交稅與滑價。
- 本輪主要資料來源仍是工作區本地 `FinMind TaiwanStockPrice` 快取、`twse_prices_close.csv`、`twse_backtest_summary.csv`、`twse_monthly_holdings.csv` 與 `twse_backtest_report.md`。
- 官方市場狀態依據 TWSE `114` 年市場開休市日期表；`2026-05-01` 為勞動節休市。
- 本輪沒有把任何休市日外部頁面數字或估值寫入回測淨值，因為可重現性不足且不符合主策略規則。

## 來源

- 工作區本地檔案：`twse_backtest_report.md`、`twse_backtest_summary.csv`、`twse_monthly_holdings.csv`、`twse_prices_close.csv`
- 回測腳本：`twse_backtest.py`
- 價格快取：`data/finmind/*`
- 官方休市表：TWSE `114` 年市場開休市日期表
- API 與快取交叉檢查：`FinMind TaiwanStockPrice`
