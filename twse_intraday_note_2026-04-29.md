# TWSE 10:00 盤中策略分析

日期：2026-04-29  
分析時間：10:00 左右  
性質：研究紀錄，不保證收益

## 摘要

- `2026-04-29 10:00` 已重跑工作區策略分析與本地資料整理；但本輪可重複驗證的最新收盤資料仍停留在 `2026-04-23`，因此今天的 10:00 盤中分析不宣稱已取得 `2026-04-24` 到 `2026-04-29` 的官方日線回測數值。
- 回測主報告顯示執行日與最新可驗證收盤日之間已有 `6` 個日曆天落差；本輪的實質新資訊是確認這個落差來自外部資料源延遲，而不是回測腳本或本地快取漏算。
- 直接抽查 FinMind API，在請求 `end_date=2026-04-29` 時，`0050` 最新回傳日仍為 `2026-04-23`；同輪 shell 直抓 TWSE `MI_INDEX` 與 `MI_5MINS_INDEX` HTML endpoint 仍回傳空內容，所以今天不能把官方頁面當成可重現的盤中驗證源。
- 以 `2026-04-23` 最近可驗證收盤檢查，`0050` 仍高於 `20` / `60` / `200` 日均線，且 `20` 日均線高於 `60` 日均線，`00631L` 的 regime 啟用條件仍成立。
- 主策略 `006208核心現金防守 50/35/10（成本0.20%）` 回測結果仍為年化 `22.47%`、最大回撤 `-24.87%`、Sharpe `1.23`；AI 動能籃子前 `5` 名仍是 `2383 / 2308 / 2345 / 2454 / 2330`。
- 本輪同步修正 `twse_backtest.py` 的 FinMind 快取命名邏輯，未來改以「實際回傳最後一筆日期」落檔，避免快取檔名誤導資料已更新。

## 今日判讀

### 市場 regime

- `risk-on` 維持。
- `006208` 核心 `50%` 不變。
- `00631L` 續留上限 `10%`，但不加碼。
- 現金緩衝維持 `5%`。

### AI 籃子

當前主策略持倉仍為：

- `2308`、`2330`、`2345`、`2383`、`2454` 各 `7%`

本輪前五名排名未變，因此沒有新的收盤版訊號支持換股。

### 風控重點

- 歷史最大回撤 `-24.87%` 仍接近內部觀察線 `-25%`，槓桿紀律維持不變。
- 當本地可驗證收盤資料落後超過 `2` 個交易日，策略只允許做監控與回測重跑，不允許依未驗證外部數字改變候選池、槓桿或現金權重。
- 當外部日線資料源落後超過 `3` 個日曆天，盤中紀錄必須同步附上 API 最新實際日期與資料凍結說明，直到本地可驗證收盤恢復更新。
- 若後續 `0050` 收盤跌破 `60` 日均線，或 `20` 日均線下穿 `60` 日均線，`00631L` 應先歸零。
- 若主策略實際回撤擴大到 `-35%`，應切換到防守模式；若擴大到 `-40%`，應視為結構失效並停止把它當主方案。
- 若補入總報酬、月營收或更細成本後，主策略年化報酬掉到 `20%` 以下，需把主策略降回候選。

## 假設與限制

- 本地回測資料截止 `2026-04-23` 收盤。
- 回測為價格報酬，未納入股息再投入。
- 交易成本壓力測試仍使用每 `1.00` 換手成本 `0.20%` 的粗略假設，尚未拆分股票、ETF、證交稅與滑價。
- 本輪主要資料來源仍是工作區本地 `FinMind TaiwanStockPrice` 快取、`twse_prices_close.csv`、`twse_backtest_summary.csv`、`twse_monthly_holdings.csv` 與 `twse_backtest_report.md`。
- API 抽查顯示，在請求 `end_date=2026-04-29` 時，`0050` 最新回傳日仍為 `2026-04-23`；今天不把任何未落地盤中成交直接混入回測淨值。
- TWSE `MI_INDEX` 與 `MI_5MINS_INDEX` HTML endpoint 在本輪 shell 抽查下回傳空內容，本紀錄把它視為資料源限制，而不是市場異常訊號。

## 來源

- 工作區本地檔案：`twse_backtest_report.md`、`twse_backtest_summary.csv`、`twse_monthly_holdings.csv`、`twse_prices_close.csv`
- 回測腳本：`twse_backtest.py`
- 價格快取：`data/finmind/*`
- API 抽查：`FinMind TaiwanStockPrice`
